name: Build Typst document

on:
  push:
    branches:
      - sources

jobs:
  build_typst:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          ref: sources

      - name: Debug Directory Contents
        run: |
          ls -R

      - name: Build Typst documents
        run: |
          docker run -e "XDG_CACHE_HOME=/root" --user $(id -u):$(id -g) -v ${{ github.workspace }}:/root --rm ghcr.io/typst/typst:latest find docs -type f -name "*\.typ" -exec sh -c 'typst compile --root . "$1" && rm -rf typst/packages/preview/' _ {} \;

      - name: Commit and push PDF files to documents branch
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
      
          # Check the status before committing
          git status
      
          git add docs/**/*.pdf
          git commit -m "Update PDF files [skip ci]"
      
          # Debug: Display changes after commit
          git status
      
          git push origin sources
      
          # Debug: Display changes after push
          git status
      
          git checkout -b documents
      
          # Debug: Display changes after checkout
          git status
      
          git checkout sources -- docs/**/*.pdf
      
          # Debug: Display changes after checkout from sources
          git status
      
          git add docs/**/*.pdf
      
          # Debug: Display changes after add
          git status
      
          git commit -m "Update PDF files [skip ci]"
      
          # Debug: Display changes after the second commit
          git status
      
          git push origin documents
      
          # Debug: Display changes after push to documents
          git status

